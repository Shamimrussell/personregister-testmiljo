# docker-compose.yml
services:
  app:
    build: .
    container_name: gdpr-user-registry
    volumes:
      - database_data:/app/data  # Named volume till /app/data
    environment:
      - DATABASE_PATH=/app/data/test_users.db
    restart: unless-stopped

volumes:
  database_data:  # Definierar volymen